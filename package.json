{
  "name": "soulbound-flare-mvp",
  "version": "0.1.0",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network localhost",
    "test": "hardhat test"
  },
  "devDependencies": {
    "hardhat": "^2.12.0",
    "@nomiclabs/hardhat-ethers": "^2.0.6",
    "ethers": "^5.7.0",
    "@openzeppelin/contracts": "^4.8.0",
    "mocha": "^10.0.0",
    "chai": "^4.3.7"
  }
}